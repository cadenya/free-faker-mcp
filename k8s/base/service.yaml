apiVersion: v1
kind: Service
metadata:
  name: faker-mcp
  labels:
    app.kubernetes.io/name: faker-mcp
spec:
  selector:
    app.kubernetes.io/name: faker-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
